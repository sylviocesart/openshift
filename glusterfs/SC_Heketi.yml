apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: gluster
provisioner: kubernetes.io/glusterfs
parameters:
  resturl: "http://10.8.2.182:8080"
  restuser: "admin"
  restuserkey: "Heketi123adm"

